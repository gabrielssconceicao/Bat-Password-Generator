import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 20, 
    width: '100%', 
    backgroundColor: '#4d4d4d',
    borderRadius: 10,
    marginBottom: 10
  },
  bar: {
    height: 20,
    borderRadius: 10,
  }
});