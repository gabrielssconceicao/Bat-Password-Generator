import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    backgroundColor: '#e5bf3c',
    color: '#111',
    width: '100%',
    padding: 10,
    fontSize: 20,
    height: 50,
    borderWidth: 1,
    borderRadius: 5,
    borderColor: '#111',
    textAlign: 'center'
  }
});